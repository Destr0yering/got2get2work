import type { FastifyInstance } from "fastify"; import { ErrorEnvelopeSchema, VehicleBodySchema, VehicleSchema, type VehicleBody } from "../../../../../packages/contracts/src"; import { createAuthGuards } from "../membership/auth"; import type { FirebaseTokenVerifier } from "../membership/ports"; import type { MembershipService } from "../membership/service"; import type { VehicleService } from "./service";
export interface VehicleRouteDependencies { verifier: FirebaseTokenVerifier; memberships: MembershipService; vehicles: VehicleService }
export async function registerVehicleRoutes(app: FastifyInstance, d: VehicleRouteDependencies) { const auth = createAuthGuards(d.verifier, d.memberships).requireActiveMembership; app.get("/v1/vehicle", { preHandler: auth, schema: { tags: ["vehicle"], response: { 200: VehicleSchema, 404: ErrorEnvelopeSchema } } }, async (r) => d.vehicles.get(r.identity!)); app.put<{ Body: VehicleBody }>("/v1/vehicle", { preHandler: auth, schema: { tags: ["vehicle"], body: VehicleBodySchema, response: { 200: VehicleSchema, 400: ErrorEnvelopeSchema } } }, async (r) => d.vehicles.save(r.identity!, r.body)); }
