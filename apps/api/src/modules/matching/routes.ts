import type { FastifyInstance } from "fastify"; import { ErrorEnvelopeSchema, MatchSearchBodySchema, MatchSearchResultSchema, type MatchSearchBody } from "../../../../../packages/contracts/src"; import { createAuthGuards } from "../membership/auth"; import type { FirebaseTokenVerifier } from "../membership/ports"; import type { MembershipService } from "../membership/service"; import type { MatchingService } from "./service";
export interface MatchingRouteDependencies { verifier: FirebaseTokenVerifier; memberships: MembershipService; matching: MatchingService }
export async function registerMatchingRoutes(app: FastifyInstance, d: MatchingRouteDependencies) { const auth = createAuthGuards(d.verifier, d.memberships).requireActiveMembership; app.post<{ Body: MatchSearchBody }>("/v1/matches/search", { preHandler: auth, schema: { tags: ["matching"], body: MatchSearchBodySchema, response: { 200: MatchSearchResultSchema, 400: ErrorEnvelopeSchema, 409: ErrorEnvelopeSchema } } }, async (r) => d.matching.search(r.identity!, r.body.shiftId, r.body.leg)); }
