import type { MatchRunRecord } from "./domain"; import type { MatchingStore } from "./ports";
const pair = (a: string, b: string) => [a, b].sort().join("_");
export class MemoryMatchingStore implements MatchingStore { readonly restrictions = new Set<string>(); readonly userRestrictions=new Map<string,Date>(); readonly runs: MatchRunRecord[] = []; restrict(a: string, b: string) { this.restrictions.add(pair(a, b)); } async isRestricted(_tenantId: string, a: string, b: string) { return this.restrictions.has(pair(a, b))||[a,b].some(uid=>(this.userRestrictions.get(uid)?.getTime()??0)>Date.now()); } async block(_tenantId: string, a: string, b: string, now: Date) { this.restrict(a, b); return { id: pair(a, b), createdAt: now }; } async restrictUser(_tenantId:string,uid:string,through:Date){this.userRestrictions.set(uid,through);} async saveRun(run: MatchRunRecord) { this.runs.push(structuredClone(run)); } async getRun(id: string) { const run = this.runs.find(v => v.id === id); return run ? structuredClone(run) : null; } }
