import type { RideRequestRecord } from "./domain";
export interface RideStore { create(record: RideRequestRecord): Promise<RideRequestRecord>; get(id: string): Promise<RideRequestRecord | null>; listForParticipant(uid: string, limit: number): Promise<RideRequestRecord[]>; listForTenant(tenantId: string, worksiteId: string, from: Date, through: Date): Promise<RideRequestRecord[]>; agree(id: string, crewUid: string, now: Date): Promise<RideRequestRecord>; decide(id: string, captainUid: string, accepted: boolean, now: Date): Promise<RideRequestRecord>; cancel(id: string, participantUid: string, reason: string, now: Date): Promise<RideRequestRecord>; }
