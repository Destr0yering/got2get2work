import type { FastifyInstance } from "fastify"; import { ErrorEnvelopeSchema, MessageBodySchema, MessageListSchema, MessageSchema, TripDetailSchema } from "../../../../../packages/contracts/src"; import { createAuthGuards } from "../membership/auth"; import type { FirebaseTokenVerifier } from "../membership/ports"; import type { MembershipService } from "../membership/service"; import type { TripService } from "./service";
export interface TripRouteDependencies { verifier: FirebaseTokenVerifier; memberships: MembershipService; trips: TripService }
const params = { type: "object", required: ["id"], properties: { id: { type: "string", minLength: 1, maxLength: 128 } } };
export async function registerTripRoutes(app: FastifyInstance, d: TripRouteDependencies) { const auth = createAuthGuards(d.verifier, d.memberships).requireActiveMembership; app.get<{ Params: { id: string } }>("/v1/trips/:id", { preHandler: auth, schema: { tags: ["trips"], params, response: { 200: TripDetailSchema, 404: ErrorEnvelopeSchema } } }, async (r) => d.trips.get(r.identity!, r.params.id)); app.get<{ Params: { id: string } }>("/v1/trips/:id/messages", { preHandler: auth, schema: { tags: ["messages"], params, response: { 200: MessageListSchema, 404: ErrorEnvelopeSchema } } }, async (r) => ({ messages: await d.trips.listMessages(r.identity!, r.params.id) })); app.post<{ Params: { id: string }; Body: { text: string } }>("/v1/trips/:id/messages", { preHandler: auth, schema: { tags: ["messages"], params, body: MessageBodySchema, response: { 201: MessageSchema, 400: ErrorEnvelopeSchema, 404: ErrorEnvelopeSchema } } }, async (r, reply) => reply.status(201).send(await d.trips.sendMessage(r.identity!, r.params.id, r.body.text))); }
