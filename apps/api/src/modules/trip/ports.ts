import type { CompletionRecord, MessageRecord, NotificationRecord, PickupStateRecord, ProximityRecord, RatingRecord, SafetyReportRecord } from "./domain";
export interface TripStore { listMessages(tripId: string, limit: number): Promise<MessageRecord[]>; saveMessage(message: MessageRecord, notification: NotificationRecord): Promise<MessageRecord>; getPickupState(tripId: string): Promise<PickupStateRecord | null>; savePickupState(state: PickupStateRecord): Promise<PickupStateRecord>; getProximity(tripId: string): Promise<ProximityRecord | null>; saveProximity(record: ProximityRecord): Promise<ProximityRecord>; deleteProximity(tripId: string): Promise<void>; saveCompletion(record: CompletionRecord): Promise<CompletionRecord>; getCompletion(tripId: string, uid: string): Promise<CompletionRecord | null>; saveRating(record: RatingRecord): Promise<RatingRecord>; saveSafetyReport(record: SafetyReportRecord): Promise<SafetyReportRecord>; }
