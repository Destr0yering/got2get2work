import type { DataRequestRecord, ModerationAuditRecord, ModerationCaseRecord } from "./domain";
export interface CaseTransition { expectedVersion:number; expectedStatuses:ModerationCaseRecord["status"][]; expectedAssignee:string|null|"any"; next:ModerationCaseRecord; audit:ModerationAuditRecord; restriction?:{uid:string;through:Date;createdAt:Date} }
export interface OperationsStore { createCase(v:ModerationCaseRecord):Promise<void>; listCases(tenantId:string,limit:number):Promise<ModerationCaseRecord[]>; getCase(id:string):Promise<ModerationCaseRecord|null>; transitionCase(v:CaseTransition):Promise<void>; createOrGetDataRequest(v:DataRequestRecord):Promise<DataRequestRecord>; listDataRequests(uid:string):Promise<DataRequestRecord[]>; }
